# Copy this file to .env and fill it in (cp .env.example .env)
#
# Miaoxiang API key (Eastmoney paid API, optional). Paste it after the equals sign (no quotes), or leave blank.
# If blank, social financing data comes directly from the PBoC website with AKShare as fallback. Nothing breaks.
# Get a key at: https://dl.dfcfs.com/m/itc4
export MX_APIKEY=
