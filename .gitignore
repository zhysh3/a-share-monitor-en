# Secrets — never commit
.env

# Python virtual environment (166MB, build per platform)
venv/
__pycache__/
*.pyc

# Runtime logs and state
logs/
*.log
.arisk_nontrading_dates

# Machine-specific launchd config (contains absolute paths); only the .example template is committed
com.arisk.update.plist

# Downloaded third-party docs
miaoxiang_doc.md

# OS clutter
.DS_Store
